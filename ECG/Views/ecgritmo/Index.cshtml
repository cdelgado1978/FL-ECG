@model IEnumerable<ECG_DB.ecg_ritmo>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ritmo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ritmo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ritmo_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.ritmo_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ritmo_id })
        </td>
    </tr>
}

</table>
